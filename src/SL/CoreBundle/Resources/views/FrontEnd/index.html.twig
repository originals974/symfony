{% extends 'SLCoreBundle:FrontEnd:frontEnd.html.twig' %}

{% block head_title %}
    {{ parent() }} - {{ 'frontEnd.welcome'|trans }}
{% endblock %}

{% block search_section %}
    {{ parent() }}

    <div class="row">
	    {{ form_start(form) }}

	    	{{ form_errors(form) }}

		    {{ form_widget(form.searchField) }}
	        {{ form_widget(form.submit) }}
		    
		{{ form_end(form) }}
	</div>

	<div class="row">
		<div id="jstree"></div>
	</div>
{% endblock %}
