{% set panelClass = 'panel panel-info' %}

{% embed 'SLCoreBundle::sectionSkeleton.html.twig' with {
    'panelClass':panelClass,
    } only
%}

    {% set title = 'object.index.title' %}
    {% set description = 'object.index.description' %}

    {% block section_heading %}
	    <div class="btn-group">              
            <a title="{{ 'object.add'|trans }}" class="btn btn-default btn-xs" data-toggle="modal" data-target="#" href="{{ path('object_new') }}">
                <span class="fa fa-plus"></span>  
            </a>       
        </div>
        <h4>{{ title|trans }}</h4>
    {% endblock %}

    {% block section_body %}
       {{ description|trans }}
    {% endblock %}

{% endembed %}
